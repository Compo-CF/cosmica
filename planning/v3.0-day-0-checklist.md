# Cosmica 3.0 — Day-0 Checklist

The exact sequence for the moment v2.1.1 (build 18) goes Ready-for-Sale on the App Store. About 10 minutes of your time, then I take over on Phase 1 code.

## Step 1: Confirm v2.1.1 is live (30 sec)

- Open [App Store Connect → Cosmica](https://appstoreconnect.apple.com) → App Store tab → 2.1.1 row shows **Ready for Sale**
- Sanity-check on your phone: App Store → search "Cosmica: Idle Universe" → the update pill or Version History shows 2.1.1
- If a lifetime-past-3.8e33 tester (or your own dev save) confirms shards climb past 9.2e18: bug is dead. Confirm.

## Step 2: Create the Automation Core IAP in ASC (~5 min)

- [App Store Connect → Cosmica](https://appstoreconnect.apple.com) → **Monetization → In-App Purchases** → **+**
- **Type:** Non-Consumable
- **Reference Name:** `Automation Core` (internal, not shown to users)
- **Product ID:** `automation_core` (must match exactly — I'll use this string in code)
- **Price:** Tier 3 ($2.99 USD, auto-converts worldwide)
- **Availability:** All Countries or Regions (matches app availability)
- **Display Name (localization → English (U.S.))**: `Automation Core`
- **Description:** `Unlock Automation — buy generators, Big Bang, and collect events without tapping.`
- **Review Screenshot:** hold off. I'll help you generate one during Phase 3 once the Big Bang tab has the Auto-Big-Bang section visible — that's the cleanest single-screenshot demo of what the IAP unlocks
- Leave **Family Sharing** ON (standard for non-consumables)
- Click **Save** — status will be "Missing Metadata" until we add the screenshot. That's fine for now — sandbox purchases work with Missing Metadata

**Confirm the product ID exactly matches `automation_core`** (all lowercase, one underscore). This is the string I'll hardcode in `IAPManager.swift`.

## Step 3: Tell me to start Phase 1

Just message me: **"start phase 1"** or similar. I'll:

1. Pull `main` (should be at commit `d8e6edc` — the planning-docs push from tonight)
2. Add `automation_core` to `IAPManager.swift` alongside the existing 5 base IAPs
3. Create `Cosmica/Services/AutomationManager.swift` (new file, `@Observable` service holding runtime state)
4. Add `automationTrialExpiresAt: Date?` to `GameState.swift` (with `decodeIfPresent` for save-compat)
5. Wire `AutomationManager` into the environment chain in `CosmicaApp.swift`
6. Add the Settings row to `SettingsView.swift` under Purchases: "Automation Core — Owned / Trial (Xh Ym left) / Not owned"
7. Wire the rewarded-ad callback in `ShopView.swift` (or wherever rewarded-ad reward-granting lives) to call `AutomationManager.grantTrial(hours: 4)` behind a "Try Automation for 4 hours" reward label
8. Commit as `v3.0.0 build 19 (phase 1): Automation Core IAP + scaffold`
9. Bump project.yml to 3.0.0 / build 19
10. Push to GitHub

Then hand you the Mac commands for the first TestFlight of the 3.0 train:

```bash
cd ~/cosmica && git pull && ~/bin/xcodegen generate
```

Archive + upload build 19.

**Expected total time for Phase 1: ~4 hours of my work + your ~15 min of Mac archive time.**

Ships as a **no-op** — the IAP is purchasable but nothing yet uses it. That's intentional. Ship-as-no-op is a valid TestFlight state and lets us prove the purchase flow works before Phase 2 depends on it.

## Step 4: Watch v2.1.1 crash metrics for 3 days before I start Phase 2

Reason: v2.1.1 changed shards from Int to Double throughout the compute path. Extremely unlikely to introduce a crash but not impossible. Give the fix 72 hours of real-world observation before we stack Phase 2 changes on top of that same compute path.

If crash metrics are clean at 72 hours: green light Phase 2, I start immediately after your "start phase 2" message.
If a new crash appears: hotfix it as v2.1.2 first, then resume.

## Step 5 (parallel, no dev dependency): Dispatch v2.0 email

While Phase 1 is scaffolding, the Dispatch v2.0 email is STILL not sent (from the memory — pending since the v2.0 push). Now's actually a great moment for it — the shift-into-worldwide angle + the shard cap fix + the v3.0 automation teaser all together make one coherent issue:

- **Subject:** "Cosmica goes worldwide (and gets a lot smarter next month)"
- **Body angle:** brief recap of what shipped (v2.0 content + monetization tuning), the shard cap fix for endgame players, the worldwide availability flip, and a one-line teaser for 3.0 Automation
- **CTA:** App Store link, and "reply to this email if there's a feature you want in 3.0 — I'm listening"
- Schedule for the morning after v2.1.1 goes live so the update banner is fresh

Draft is in your Beehiiv (or I can write a fresh one — let me know)

## Assumptions I'm making

- You want to ship v3.0 as fast as possible without cutting scope; if you want to defer Phase 5 or 6 to 3.0.1, say so before I start those phases
- You want per-phase TestFlight betas (not one massive submit at the end); if you prefer one big submit, say so at Phase 1 start
- Automation Core stays at Tier 3 ($2.99) — if analytics from v2.0/v2.1 suggest a different price would convert better, say so before I create the IAP; changing price after launch is possible but requires an IAP metadata refresh

## What I will NOT do without your explicit go

- Touch anything on the Mac side (all archive/upload/submit remains yours)
- Change any App Store metadata on live versions
- Add or modify any IAP after `automation_core` is created — future IAPs (3.1 Multiverse pack, 3.2 Cosmetics bundle) wait for their own explicit approval
- Push to any branch other than `main`
- Enable auto-merge on any PR (per your standing preference)
- Ship any half-built feature (per [[feedback-build-it-properly]] — no permissions or declarations without implementation)
